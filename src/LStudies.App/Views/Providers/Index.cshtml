@using LStudies.App.Extensions
@model IEnumerable<LStudies.App.ViewModels.ProviderViewModel>

@{
    ViewData["Title"] = "Providers List";
}

<h1>@ViewData["Title"]</h1>
<hr />

<p supress-by-claim-name="Providers" supress-by-claim-value="Create">
    <a class="btn btn-info" asp-action="Create">New Provider</a>
</p>
<table class="table table-hover">
    <thead class="thead-dark">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Document)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsActive)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @this.FormatDocument(item.ProviderType, item.Document)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IsActive)
                </td>
                <td class="text-right">
                    <a class="btn btn-info" asp-action="Details" asp-route-id="@item.Id"><span class="fa fa-search"></span></a>
                    <a class="btn btn-warning" asp-action="Edit" asp-route-id="@item.Id" disable-by-claim-name="Providers" disable-by-claim-value="Edit"><span class="fa fa-pencil-alt"></span></a>
                    <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.Id" supress-by-claim-name="Providers" supress-by-claim-value="Delete"><span class="fa fa-trash"></span></a>
                </td>
            </tr>
        }
    </tbody>
</table>
